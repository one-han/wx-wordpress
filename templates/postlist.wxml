<template name="postlist">
  <view class="weui-panel__bd">
    <view wx:for="{{posts}}" wx:key="pkey">
      <post title="{{item.title}}" desc="{{item.desc}}" time="{{item.time}}" likes="{{item.likes}}" postid="{{item.id}}" />
    </view>
  </view>
</template>